[project]
name = "pfun-health-github-io"
version = "0.1.0"
description = "github pages repo for pfun-health"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "zensical>=0.0.29",
]
